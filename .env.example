# Okta IGA MCP Server Configuration

# Your Okta org domain (e.g., https://your-org.okta.com)
OKTA_DOMAIN=https://your-org.okta.com

# OAuth 2.0 Service App Client ID
OKTA_CLIENT_ID=your-client-id

# Private key for JWT assertion (PEM format, newlines as \n)
# Or use OKTA_PRIVATE_KEY_PATH to point to a file
OKTA_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----"
# OKTA_PRIVATE_KEY_PATH=/path/to/private-key.pem

# OAuth scopes (space-separated) - read and write scopes for IGA
OKTA_SCOPES=okta.governance.accessCertifications.read okta.governance.accessCertifications.manage okta.governance.accessRequests.read okta.governance.accessRequests.manage okta.governance.entitlements.read

# Optional: Auth server ID (default: empty for org authorization server)
# OKTA_AUTH_SERVER_ID=default

# Optional: HTTP timeout in ms (default: 30000)
# OKTA_TIMEOUT_MS=30000

# Optional: Max retries on 429/5xx (default: 3)
# OKTA_MAX_RETRIES=3
